name: crypto_storage_impl
description: A new Flutter package.
version: 0.0.1
homepage:
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto_model:
    path: ../crypto_model
  crypto_storage:
    path: ../crypto_storage
  flutter_secure_storage: ^10.3.1
  #move all db to module
  aurora_mail:
    path: ../../..
  crypto_worker:
    path: ../crypto_worker

dev_dependencies:
  flutter_test:
    sdk: flutter
